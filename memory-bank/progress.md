# Progress

## What is Working
- `rabbitmesh` client RPC execution, RabbitMQ binding logic, Request/Response payload marshaling.
- Advanced procedural macros producing router tables and JSON definitions.
- Axum gateway acting as an automated proxy.
- Node.js SDK Generation from compiled metadata.

## Pending Completion
- [ ] Connect Dynamic macros router metadata dynamically into the `axum` registry.
- [ ] Example e-commerce microservices (Orders, Auth, Users) need full lifecycle completion using a database.
- [ ] Complete handling of service states to retrieve RMQ instances dynamically (Global state issue).
